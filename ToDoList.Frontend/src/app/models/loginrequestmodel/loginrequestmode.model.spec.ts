import { Loginrequestmode } from './loginrequestmode.model';

describe('Loginrequestmode', () => {
  it('should create an instance', () => {
    expect(new Loginrequestmode()).toBeTruthy();
  });
});
