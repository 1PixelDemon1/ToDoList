import { AspPasswordValidatorDirective } from './asp-password-validator.directive';

describe('AspPasswordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AspPasswordValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
