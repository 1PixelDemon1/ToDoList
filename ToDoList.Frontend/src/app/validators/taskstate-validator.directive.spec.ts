import { TaskstateValidatorDirective } from './taskstate-validator.directive';

describe('TaskstateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskstateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
